<%- include('partials/header') %>

<section class="container py-4">
  <!-- Breadcrumb -->
  <p class="text-muted small mb-2">
    <a href="/" class="text-decoration-none text-success">Home</a> › <%= category === 'All' ? 'All Products' : category %>
  </p>

  <!-- Page heading -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h2 class="fw-bold"><%= category === 'All' ? 'All Products' : category %></h2>
      <p class="text-muted small">
        Browse our wide range of products — fresh, local, and delivered across Singapore.
      </p>
    </div>
    <div class="dropdown">
      <button class="btn btn-outline-success dropdown-toggle" data-bs-toggle="dropdown">Sort by</button>
      <ul class="dropdown-menu">
        <li><a class="dropdown-item" href="#">Recommended</a></li>
        <li><a class="dropdown-item" href="#">Price: Low to High</a></li>
        <li><a class="dropdown-item" href="#">Price: High to Low</a></li>
      </ul>
    </div>
  </div>

  <div class="row">
    <!-- LEFT FILTER PANEL -->
    <aside class="col-lg-3 mb-4">
      <div class="border rounded-3 p-3 bg-white shadow-sm">
        <h6 class="fw-bold text-success mb-3">Browse by</h6>
        <ul class="list-unstyled">
          <% categories.forEach(cat => { %>
            <li class="mb-2">
              <a href="/shopping?category=<%= cat %>"
                 class="text-decoration-none <%= category === cat ? 'fw-semibold text-success' : 'text-dark' %>">
                <%= cat %>
              </a>
            </li>
          <% }) %>
        </ul>

        <hr>
        <h6 class="fw-bold text-success mb-3">Filter by</h6>
        <p class="small mb-1 text-muted">Price</p>
        <input type="range" class="form-range" min="1" max="100" id="priceRange">
        <div class="d-flex justify-content-between small">
          <span>$1</span><span>$100</span>
        </div>

        <p class="small text-muted mt-3">Size</p>
        <p class="small text-muted">Weight</p>
      </div>
    </aside>

    <!-- PRODUCT GRID -->
    <div class="col-lg-9">
      <% if (products && products.length > 0) { %>
        <div class="row row-cols-2 row-cols-md-3 row-cols-lg-3 g-4">
          <% products.forEach(product => { %>
            <div class="col">
              <div class="card border-0 product-card h-100 shadow-sm">
                <img src="/images/<%= product.image %>" class="card-img-top p-3" 
                     alt="<%= product.productName %>" style="height:180px;object-fit:contain;">
                <div class="card-body text-center d-flex flex-column">
                  <h6 class="fw-semibold"><%= product.productName %></h6>
                  <p class="fw-bold text-success mb-2">$<%= product.price.toFixed(2) %></p>
                  <form action="/add-to-cart/<%= product.id %>" method="POST" class="mt-auto">
                    <input type="hidden" name="quantity" value="1">
                    <button class="btn btn-outline-success btn-sm w-100">Add to Cart</button>
                  </form>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p class="text-muted text-center">No products found in this category.</p>
      <% } %>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
